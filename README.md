# Gate Controller

This repository contains several iterations of the Gate Controller project. Cloud-v3 and the `agent` directory are actively maintained; the `cloud` and `cloud-v2` directories are kept for historical reference.

See the README in `cloud-v3/` for setup instructions.
For details on the hardware agent, see the README in `agent/`.
