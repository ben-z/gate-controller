# Gate Controller

This repository contains several iterations of the Gate Controller project. Only **cloud-v3** is actively maintained; the `cloud` and `cloud-v2` directories are kept for historical reference.

See the README in `cloud-v3/` for setup instructions.
